# Claude API — all document reading, classification, extraction and drafting
ANTHROPIC_API_KEY=

# OpenAI — embeddings only (pgvector SKU shortlisting)
OPENAI_API_KEY=

# PostgreSQL (docker-compose default below)
DATABASE_URL=postgresql+psycopg://bidpilot:bidpilot@localhost:5433/bidpilot

# Gmail IMAP — requires 2FA + App Password (normal passwords fail on IMAP)
GMAIL_USER=
GMAIL_APP_PASSWORD=
